Register
--------
* we can't declare register variable globally.

* Default value is a garbage.

* It is stored in internal CPU registers or stack

* We can't use address operator to register storage class
	
	scope -> within a block

	life  -> when the function execution life starts, when the function execution ends life ends
